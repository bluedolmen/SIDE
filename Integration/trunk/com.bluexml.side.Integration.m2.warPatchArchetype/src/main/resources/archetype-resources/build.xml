<project name="${artifactId}">
	<property file="build.${user.name}.properties" />
	<property name="tmpFolder" value="./tmp" />
	<property name="extensionFolder" value="./target" />
	<property name="resources" value="./resources" />
	<property name="module.properties" value="${resources}/module.properties" />
	<property file="${module.properties}" />
	<property name="module.jar" value="./lib/${module.id}.jar" />
	<property name="module.zip" value="${extensionFolder}/${module.id}-${module.version}.zip" />

	<property name="packageDescriptionFile" value="moduleDescriptions/module.${module.id}.properties" />
	<!-- ================================= 
          target: buildExtension              
         ================================= -->
	<target name="buildExtension" depends="clean" description="build extension archive for alfresco share">
		<echo message="${module.version}">
		</echo>
		<delete dir="${tmpFolder}" failonerror="false" />
		<mkdir dir="${extensionFolder}" />
		<!-- TODO: Insert your custom code mybe uncomment jar building ...-->

		<!--jar destfile="${module.jar}">
			<fileset dir="./target/classes" id="id">
				<include name="**/*.*"/>
			</fileset>
		</jar-->
		<copy file="${module.properties}" tofile="${tmpFolder}/${packageDescriptionFile}" />
		<copy todir="${tmpFolder}">
			<fileset dir="${resources}" id="id">
				<exclude name="${module.properties}" />
			</fileset>
		</copy>
		<zip destfile="${module.zip}">
			<fileset dir="${tmpFolder}" id="id">
				<include name="**/*.*" />
			</fileset>
		</zip>
	</target>


	<!-- ================================= 
          target: copyTo
         ================================= -->
	<target name="copyTo" depends="buildExtension" description="build and copy package into generated folder (same as generator do)">
		<copy todir="${generated.home}" overwrite="true">
			<fileset dir="${extensionFolder}">
				<include name="*.zip" />
			</fileset>
		</copy>
	</target>


	<!-- ================================= 
          target: clean              
         ================================= -->
	<target name="clean" depends="" description="delete tmp and builded extension">
		<delete dir="${tmpFolder}" failonerror="false" />
		<delete dir="${extensionFolder}" failonerror="false" />
	</target>

</project>